All code, comments, and documentation must be written in English.

When you need to release a new version (whether major, minor, or patch), simply update the version number in `package.json`. There is no need to run `npm version`.